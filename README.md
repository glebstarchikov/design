# design

The unified design system for everything I ship. Read **[design.md](design.md)**.

Copy [tokens.css](tokens.css) into a project to adopt it; run [contrast.py](contrast.py) to verify any theme against the contrast floor.
